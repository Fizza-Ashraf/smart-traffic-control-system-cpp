# smart-traffic-control-system-cpp
 Smart traffic light control system implemented in C++ using data structures. The system simulates real-time traffic management using sensor-based input to detect vehicle density and dynamically prioritizes lanes based on traffic load to optimize signal timing and improve traffic flow efficiency.
